<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Dashboard" />
    </group>

    <item android:title="Meine Server" android:checkableBehavior="single">
        <menu></menu>
    </item>

    <item android:title="Kommunikation">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Teile uns" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Fehler melden" />
        </menu>
    </item>

    <item
        android:id="@+id/navLogout"
        android:icon="@drawable/ic_action_exit"
        android:title="Ausloggen" />

</menu>
